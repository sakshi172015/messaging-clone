let mode = "dark"

let colors = {
    task_bar: mode === "dark" ? "#1F2C34" : "#F1F2F6",
    chat_menu: mode === "dark" ? "#121B22" : "#FFFFFF",
    selected_chat: mode === "dark" ? "#2B3942" : "#F1F2F6",
    received_chat: mode === "dark" ? "#1F2C34" : "#FFFFFF",
    send_chat: mode === "dark" ? "#005D4B" : "#D8FDD2",
    send_message_box: mode === "dark" ? "#2B3942" : "#FFFFFF",
    icon: mode === "dark" ? "#8A979F" : "#737C85",
    background: mode === "dark" ? "#131A22" : "#00A884"
}

export default colors